Backend -
    package installation
    Express, mongoose, nodemon, dotenv, bcrypty.js, jsonwebtoken, cookie-parser, morgan - to show api in  console


auth flow - 
user get the register page 
    - fullName, email, password, mobile, role
    - then we will call register api and store the - - data in db it will create the unique id  database 
    we  will create/genrate  the token using the id, name and role and store in  web cookie to 

    we generate the token at register/signup aslo for better user experience 
    


MongoDb atlash -setup 
    go to website 
    create the new project 
    select the plan 
    create cluster 
    copy password, 
    copy mongourl 
    